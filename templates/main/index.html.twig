{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">



<ul>
    <h2>etap 1</h2>
        {% for key, ars in etap1 %}

            {% for name, value in ars %}
                <p>{{ name }} : {{ value }}</p>
            {% endfor %}
{#            <li> {{ key }}: {{ ars.azganun }}</li>#}
{#            <li> {{ key }}: {{ ars.age }}</li>#}

        {% endfor %}
        </ul>
    <ul>
        <h2>etap 1</h2>
        {% for key, ars in etap2 %}

            {% for name, value in ars %}
                <p>{{ name }} : {{ value }}</p>
            {% endfor %}
            {#            <li> {{ key }}: {{ ars.azganun }}</li>#}
            {#            <li> {{ key }}: {{ ars.age }}</li>#}

        {% endfor %}
    </ul>
        {{ controller_name }} ✅

    This friendly message is coming from:

    <ul>
        <li>Your controller at <code><a href="{{ '/var/www/html/arsen_first/src/Controller/MainController.php'|file_link(0) }}">src/Controller/MainController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/var/www/html/arsen_first/templates/main/index.html.twig'|file_link(0) }}">templates/main/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
